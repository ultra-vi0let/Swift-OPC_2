import XCTest
@testable import Swift_OPC_2

final class Swift_OPC_2Tests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(Swift_OPC_2().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
